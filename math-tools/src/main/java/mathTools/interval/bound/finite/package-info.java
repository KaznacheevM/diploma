/**
 * Provides implementations of finite bounds for intervals.
 *
 * <p>This package contains classes that represent bounds with finite values, such as
 * {@link mathTools.interval.bound.finite.FiniteBound}, and factory
 * abstractions like {@link mathTools.interval.bound.finite.AbstractFiniteBound}.
 *
 * <p>Key classes:
 * <ul>
 *     <li>{@link mathTools.interval.bound.finite.FiniteBound} — represents a specific finite bound.</li>
 *     <li>{@link mathTools.interval.bound.finite.AbstractFiniteBound} — a base class for finite bounds.</li>
 * </ul>
 */
package mathTools.interval.bound.finite;