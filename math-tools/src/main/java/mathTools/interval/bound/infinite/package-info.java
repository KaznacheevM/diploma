/**
 * Provides implementations of infinite bounds for intervals.
 *
 * <p>This package contains classes for representing positive and negative infinity,
 * such as {@link mathTools.interval.bound.infinite.DirectionalInfiniteBound}, and
 * factory abstractions like {@link mathTools.interval.bound.infinite.AbstractInfiniteBound}.
 *
 * <p>Key classes:
 * <ul>
 *     <li>{@link mathTools.interval.bound.infinite.DirectionalInfiniteBound} — a factory implementation
 *     of infinite bounds with a fixed direction (positive or negative).</li>
 *     <li>{@link mathTools.interval.bound.infinite.AbstractInfiniteBound} — a base class for
 *     infinite bounds.</li>
 * </ul>
 */
package mathTools.interval.bound.infinite;