/**
 * Provides factories for creating instances of {@link mathTools.interval.bound.Bound}.
 *
 * <p>This package contains the {@link mathTools.interval.bound.factory.BoundFactory}
 * interface and its implementations. These factories are responsible for creating finite and
 * infinite bounds in a standardized way.
 *
 * <p>Key classes and interfaces:
 * <ul>
 *     <li>{@link mathTools.interval.bound.factory.BoundFactory} — the primary factory interface
 *     for creating bounds.</li>
 *     <li>{@link mathTools.interval.bound.factory.DefaultBoundFactory} — the default implementation
 *     of the {@link mathTools.interval.bound.factory.BoundFactory} interface.</li>
 * </ul>
 */
package mathTools.interval.bound.factory;