/**
 * Provides utilities for validating arguments against mathematical intervals.
 *
 * <p>The primary components of this package include:
 * <ul>
 *     <li>{@link mathTools.validation.interval.IntervalValidator}:
 *     A validator that ensures arguments fall within a specific interval.</li>
 *     <li>{@link mathTools.validation.interval.factory.IntervalValidatorFactory}:
 *     A factory interface for creating interval-based validators with custom bounds.</li>
 * </ul>
 *
 * <p>For predefined interval validators, see the {@code factory} subpackage.
 */
package mathTools.validation.interval;